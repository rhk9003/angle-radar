# 複製本檔為 secrets.toml 後填入實際值。
# secrets.toml 已被 .gitignore 排除，不會進 public repo。
# 部署到 Streamlit Community Cloud 時，改在網站的 Secrets 介面貼同樣內容。

GEMINI_API_KEY = "你的 Gemini API key"
YOUTUBE_API_KEY = "你的 YouTube Data API key"

# 白名單 Google Sheet（選填）。設了才會啟用試用碼閘門。
# 該 Sheet 需設「知道連結的人皆可檢視」，第一列表頭：code | name | active
WHITELIST_SHEET_ID = "Google Sheet 網址中 /d/ 與 /edit 之間那段 ID"
WHITELIST_SHEET_GID = "0"   # 分頁 gid（網址 #gid= 後面數字），預設 0
